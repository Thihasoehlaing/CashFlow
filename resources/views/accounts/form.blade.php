<x-layouts::app :title="$account->exists ? __('accounts.edit') : __('accounts.create')"><form class="card grid gap-4 md:grid-cols-2" method="POST" action="{{ $account->exists ? route('accounts.update', $account) : route('accounts.store') }}">@csrf @if($account->exists) @method('PUT') @endif <label class="form-field">{{ __('accounts.name') }}<input class="field" name="name" value="{{ old('name', $account->name) }}" required></label><label class="form-field">{{ __('accounts.type') }}<select class="field" name="type">@foreach (collect(trans('accounts.types'))->sort()->keys() as $type)<option value="{{ $type }}" @selected(old('type', $account->type)===$type)>{{ __("accounts.types.$type") }}</option>@endforeach</select></label><label class="form-field">{{ __('accounts.currency') }}<input class="field" name="currency" maxlength="3" value="{{ old('currency', $account->currency ?: 'MYR') }}"></label><label class="form-field">{{ __('accounts.account_number') }}<input class="field" name="account_number" value="{{ old('account_number', $account->account_number) }}"></label><label class="form-field">{{ __('accounts.opening_balance') }}<input class="field font-mono" name="opening_balance" type="number" step="0.01" value="{{ old('opening_balance', $account->opening_balance ?? 0) }}"></label><label class="flex items-center gap-2 text-sm text-[var(--color-text-muted)]"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $account->is_active ?? true))>{{ __('accounts.is_active') }}</label><label class="form-field md:col-span-2">{{ __('accounts.notes') }}<textarea class="field" name="notes">{{ old('notes', $account->notes) }}</textarea></label>@if ($errors->any())<div class="md:col-span-2 rounded-lg border border-[var(--color-danger)] p-4 text-sm text-[var(--color-danger)]">{{ $errors->first() }}</div>@endif <button class="btn-primary md:col-span-2">{{ __('common.save') }}</button></form></x-layouts::app>
