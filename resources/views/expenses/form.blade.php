<x-layouts::app :title="$expense->exists ? __('expenses.edit') : __('expenses.create')"><form class="card space-y-6" method="POST" action="{{ $expense->exists ? route('expenses.update', $expense) : route('expenses.store') }}">@csrf @if($expense->exists) @method('PUT') @endif @livewire('expense-form', ['expense' => $expense]) <div class="grid gap-4 md:grid-cols-2"><label class="form-field">{{ __('accounts.title') }}<select class="field" name="account_id">@foreach ($accounts as $account)<option value="{{ $account->id }}" @selected(old('account_id', $expense->account_id)==$account->id)>{{ $account->name }}</option>@endforeach</select></label><label class="form-field">{{ __('common.date') }}<input class="field" name="date" type="date" value="{{ old('date', optional($expense->date)->format('Y-m-d') ?? today()->toDateString()) }}"></label><label class="form-field">{{ __('income.type') }}<select class="field" name="type">@foreach (collect(['business' => __('income.business'), 'personal' => __('income.personal')])->sort() as $type => $label)<option value="{{ $type }}" @selected(old('type', $expense->type ?: 'personal')===$type)>{{ $label }}</option>@endforeach</select></label><label class="form-field md:col-span-2">{{ __('common.description') }}<textarea class="field" name="description">{{ old('description', $expense->description) }}</textarea></label></div>@if ($errors->any())<div class="rounded-lg border border-[var(--color-danger)] p-4 text-sm text-[var(--color-danger)]">{{ $errors->first() }}</div>@endif <button class="btn-primary">{{ __('common.save') }}</button></form></x-layouts::app>
