<x-layouts::app :title="__('quotations.title')"><div class="space-y-5"><div class="flex flex-wrap justify-between gap-3"><form class="grid gap-3 sm:grid-cols-4" method="GET"><select class="field" name="status"><option value="">{{ __('common.all_statuses') }}</option>@foreach (collect(['draft','sent','accepted','rejected'])->sortBy(fn ($status) => __("common.status.$status")) as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ __("common.status.$status") }}</option>@endforeach</select><select class="field" name="client_id"><option value="">{{ __('clients.all') }}</option>@foreach ($clients as $client)<option value="{{ $client->id }}" @selected(request('client_id')==$client->id)>{{ $client->name }}</option>@endforeach</select><input class="field" name="month" type="number" min="1" max="12" value="{{ request('month') }}" placeholder="{{ __('common.month') }}"><button class="btn-secondary">{{ __('common.filter') }}</button></form><a class="btn-primary" href="{{ route('quotations.create') }}">{{ __('quotations.create') }}</a></div><section class="card overflow-x-auto"><table class="min-w-[920px] w-full text-sm"><thead class="text-left text-[var(--color-text-muted)]"><tr><th>{{ __('quotations.number') }}</th><th>{{ __('clients.title') }}</th><th>{{ __('quotations.project') }}</th><th>{{ __('quotations.total') }}</th><th>{{ __('common.status_label') }}</th><th>{{ __('quotations.valid_until') }}</th><th></th></tr></thead><tbody>@foreach ($quotations as $row)<tr class="animate-row border-t border-[var(--color-border)] {{ isset($row->due_date) && $row->due_date->isPast() && $row->status !== 'paid' ? 'bg-[rgba(224,82,82,0.08)]' : '' }}"><td class="py-3 font-mono">{{ $row->quotation_number }}</td><td>{{ $row->client->name }}</td><td>{{ $row->project_title }}</td><td><x-currency-badge :amount="$row->total" :currency="$row->currency" /></td><td><x-status-badge :status="$row->status" /></td><td>{{ $row->valid_until->format('d M Y') }}</td><td class="space-x-3 text-right"><a class="text-[var(--color-primary)]" href="{{ route('quotations.show', $row) }}">{{ __('common.view') }}</a>@if($row->status === 'draft')<a class="text-[var(--color-primary)]" href="{{ route('quotations.edit', $row) }}">{{ __('common.edit') }}</a>@endif<a class="text-[var(--color-primary)]" href="{{ route('quotations.pdf', $row) }}">PDF</a><x-confirm-modal :action="route('quotations.destroy', $row)" /></td></tr>@endforeach</tbody></table><x-pagination :paginator="$quotations" /></section></div></x-layouts::app>
