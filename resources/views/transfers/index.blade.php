<x-layouts::app :title="__('transfers.title')"><div class="space-y-5"><div class="flex justify-end"><a class="btn-primary" href="{{ route('transfers.create') }}">{{ __('transfers.create') }}</a></div><section class="card overflow-x-auto"><table class="min-w-[860px] w-full text-sm"><thead class="text-left text-[var(--color-text-muted)]"><tr><th>{{ __('common.date') }}</th><th>{{ __('transfers.from_account') }}</th><th>{{ __('transfers.to_account') }}</th><th>{{ __('transfers.from_amount') }}</th><th>{{ __('transfers.to_amount') }}</th><th>{{ __('transfers.exchange_rate') }}</th><th>{{ __('transfers.fee') }}</th><th></th></tr></thead><tbody>@foreach ($transfers as $transfer)<tr class="animate-row border-t border-[var(--color-border)]"><td class="py-3">{{ $transfer->date->format('d M Y') }}</td><td>{{ $transfer->fromAccount->name }}</td><td>{{ $transfer->toAccount->name }}</td><td><x-currency-badge :amount="$transfer->from_amount" :currency="$transfer->from_currency" /></td><td><x-currency-badge :amount="$transfer->to_amount" :currency="$transfer->to_currency" /></td><td class="font-mono">{{ $transfer->exchange_rate }}</td><td><x-currency-badge :amount="$transfer->fee" :currency="$transfer->fee_currency ?: $transfer->from_currency" /></td><td class="text-right"><x-confirm-modal :action="route('transfers.destroy', $transfer)" /></td></tr>@endforeach</tbody></table><x-pagination :paginator="$transfers" /></section></div></x-layouts::app>
